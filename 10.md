## 修复与重构（2025-11-05 补记）

- **(Refactor)** 修复了 `article/[id].vue` 中的硬编码颜色。根据代码审计（规范 3）的警告，将骨架屏（Skeleton）占位符使用的 `bg-surface-subtle` 和 `dark:bg-surface-contrast` 替换为 `uno.config.ts` 中已定义的 `surface-light`/`surface-dark` 令牌，解决了颜色不统一的问题。
 - **(Add)** `uno.config.ts` 新增一组语义化快捷类，用于替代多处原子类堆叠，核心包括：
   - 文案/排版：`eyebrow`、`eyebrow-muted-sm`、`heading-xl`、`heading-lg`、`body-muted`、`body-muted-sm`、`text-muted-xs`、`text-muted-sm`、`typography-prose-lg`、`brand-title-xl`
   - 布局：`row-between`、`stack-3`、`stack-4`、`stack-6`、`cluster-3`、`stack-responsive-between-4`、`stack-responsive-between-6`、`layout-header`、`layout-footer-text`
   - 反馈/占位：`toast-shell`、`skeleton-surface`、`skeleton-border-60`、`skeleton-border-70`、`skeleton-border-40`、`surface-note`、`surface-dashed-note`、`surface-dashed-note-sm`、`surface-bordered-body-sm`
   - 表单：`arcanum-label`、`arcanum-input`、`arcanum-captcha`
   - 列表强调：`sidebar-rule`、`sidebar-rule-strong`
 - **(Refactor)** `src/components/ArcanumLayout.vue`：头部使用 `layout-header`，品牌标题改为 `brand-title-xl`，导航区容器用 `row-between`，页脚文字采用 `layout-footer-text` 并以 `stack-responsive-between-4` 排列；新增顶部提示 `toast-shell`。
 - **(Refactor)** `src/pages/login.vue`：表单统一为 `arcanum-label`、`arcanum-input`，验证码图片使用 `arcanum-captcha`，提交按钮 `brand-button w-full`；修正未定义类 `footer-text-block` 为 `text-muted-sm mt-8 text-center`。
 - **(Refactor)** `src/pages/about.vue`：价值主张与里程碑列表替换为 `sidebar-rule-strong`/`sidebar-rule`；标题与正文字体用 `heading-xl`、`body-muted-sm`；正文区排版统一为 `typography-prose-lg`；沟通区容器采用 `stack-responsive-between-6` 与 `stack-3`。
 - **(Refactor)** `src/pages/article/[id].vue`：
   - 头部信息区使用 `card-section-header`、`eyebrow-muted-sm`、`heading-xl`
   - 加载态与失败态采用 `skeleton-surface`、`toast-shell`；空内容提示改为 `surface-note`
   - 正文 HTML 渲染容器改为 `typography-prose-lg`
   - 评论列表项统一为 `surface-bordered-body-sm`，列表头部用 `row-between`，数量文字用 `text-muted-xs`
   - 发表评论表单使用 `arcanum-label`、`arcanum-input`
   - 侧栏头部 `row-between`，正文说明 `body-muted-sm`
 - **(Refactor)** `src/pages/index.vue`：英文眉题用 `eyebrow-muted-sm`；CTA 容器用 `stack-responsive-between-4`；精选与列表摘要统一为 `body-muted-sm`；统计文字 `text-muted-sm`；策展区容器为 `stack-responsive-between-6`。
 - **(Verify)** 启动本地预览：`http://localhost:3333/`（你本地）与 `http://localhost:3334/`（本次验证），页面在深浅色模式下排版与色彩保持一致；UnoCSS Inspector 可用：`http://localhost:3334/__unocss/`。
 - **(Compliance)** 规范 1（Vue 文件无 `<style>`）与规范 2（无硬编码颜色）继续通过；规范 3（避免原子类堆叠）显著改善，建议后续为常见模式补充更多快捷类，并在 CI 中加入静态 `class` 超过 4 项的 lint 规则。